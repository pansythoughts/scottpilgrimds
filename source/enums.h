#pragma once

// SPRITE ENUMS //
enum SPRITE_SIZES
{
    CHAR_DEFAULT_WIDTH = 64,
    CHAR_DEFAULT_LENGTH = 64
};
enum CHAR_SPRITE_ANIMATION_STATES
{
    A_IDLE,
    A_WALK,
    A_RUN,
    A_JUMP
};
enum CHAR_SPRITE_PRIMARY_STATES
{
    P_IDLE,
    P_WALK,
    P_RUN,
    P_DODGING
};
enum CHAR_SPRITE_SECONDARY_STATES
{
    S_NONE,
    S_JUMP
};
enum SPRITE_TYPES
{
    SPR_CHARACTER,
    SPR_SHADOW,
    SPR_RUN_EFFECT,
    SPR_LAND_EFFECT
};
enum EFFECTS
{
    EFF_RUN_EFFECT,
    EFF_LAND_EFFECT
};

#define MAX_NUM_PARTS 16

// CHARACTER ENUMS //
enum CHARACTERS
{
    SCOTT,
    RAMONA,
    KIM,
    STILLS
};

#define CHARACTER_SPRITE_MAX_SLOTS 8

// BACKGROUND ENUMS //
enum BACKGROUNDS
{
    B_FROZEN_SUBURBS
};
enum CHUNKS
{
    LEFT,
    RIGHT
};

// SOUND ENUMS //
#define SONG_MAX_FILES 10
enum SONGS
{
    ANOTHER_WINTER
};

// GAME ENUMS //
enum GAME_STATES
{
    TITLE_SCREEN,
    MAIN_MENU,
    CHARACTER_SELECTION,
    MAP,
    LEVEL
};
enum LEVELS
{
    L_FROZEN_SUBURBS
};

// TRANSITION //
#define FADE_DURATION 45